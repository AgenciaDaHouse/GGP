.h
  &-limiter
    margin auto
    max-width $max-width
    padding 0 1rem

  &-im
    display inline-block
    vertical-align middle

  &-anchor
    position relative
    pointer-events none
    top -10rem

  &-group
    &:after
      content ''
      display table
      clear both
